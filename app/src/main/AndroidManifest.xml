<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.demoproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Room.RoomDetails"></activity>
        <activity android:name=".Room.Lobby" />
        <activity
            android:name=".Dashboard.Home"
            android:windowSoftInputMode="adjustResize|stateVisible" />
        <activity
            android:name=".Login.Login"
            android:windowSoftInputMode="adjustResize|stateVisible" />
        <activity
            android:name=".Register.Otp"
            android:windowSoftInputMode="adjustResize|stateVisible" />
        <activity
            android:name=".Register.Phone"
            android:windowSoftInputMode="adjustResize|stateVisible" />
        <activity
            android:name=".Register.Details"
            android:windowSoftInputMode="adjustResize|stateVisible" />
        <activity
            android:name=".Register.Face"
            android:windowSoftInputMode="adjustResize|stateVisible" />
        <activity
            android:name=".Register.Mail"
            android:windowSoftInputMode="adjustResize|stateVisible" />
        <activity
            android:name=".Splash.Splash"
            android:windowSoftInputMode="adjustResize|stateVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>